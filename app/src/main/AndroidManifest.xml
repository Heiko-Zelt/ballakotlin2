<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.heikozelt.ballakotlin2">

    <application
        android:name=".BallaApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/BallaTheme">
        <!-- android:icon="@mipmap/ic_launcher" -->
        <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
        <!-- same theme for all activitities -->
        <activity android:name=".view.TogglesActivity" android:exported="true" />
        <activity android:name=".view.DimensionsActivity" android:exported="true" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true"> <!-- android:theme="@style/AppThemeActivity" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>